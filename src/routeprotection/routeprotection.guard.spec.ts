import { RouteprotectionGuard } from './routeprotection.guard';

describe('RouteprotectionGuard', () => {
  it('should be defined', () => {
    expect(new RouteprotectionGuard()).toBeDefined();
  });
});
